export type PrizesBadgeProps = {
  prizeName: string,
  prizeIconSrc: string
}

export type MembersCardProps = {
  role: string,
  department: string,
  highSchool: string,
  hobbies: string,
  message: string
}